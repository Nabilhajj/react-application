# react-application
Explorer is a responsive React single-page application that consumes a free public API and presents the data as a browsable catalogue. The project demonstrates modern React development practices, including reusable components, custom hooks, state management, routing, search, filtering, sorting, favorites, and responsive UI design.
